/*
Лаб.6
Для визначених в Лаб 5. Database методів написати відповідні сервісні методи.
У цих методах використати логування з відповідними рівнями (info, warning, trace). 
*/

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import java.util.List;

public class RoomService {
    private static final Logger logger = LoggerFactory.getLogger(RoomService.class);

    // Метод для отримання всіх кімнат
    public List<Room> getAllRooms() {
        logger.info("Fetching all rooms from the database");
        // Логіка отримання всіх кімнат
        try {
            // Отримати дані з бази
            logger.trace("Successfully fetched rooms");
            return rooms;
        } catch (Exception e) {
            logger.error("Error fetching rooms: ", e);
            throw e;
        }
    }

    // Метод для створення нової кімнати
    public void addRoom(Room room) {
        logger.info("Adding a new room: {}", room);
        try {
            // Логіка додавання кімнати
            logger.trace("Room added successfully");
        } catch (Exception e) {
            logger.warn("Failed to add room: ", e);
            throw e;
        }
    }

    // Метод для видалення кімнати
    public void deleteRoom(int roomId) {
        logger.info("Deleting room with ID: {}", roomId);
        try {
            // Логіка видалення кімнати
            logger.trace("Room deleted successfully");
        } catch (Exception e) {
            logger.error("Error deleting room: ", e);
            throw e;
        }
    }
}

/*
Explanation:
CustomerService: logging when creating, deleting and updating customers.
BookingService: logging when booking, cancelling and updating bookings.
*/
